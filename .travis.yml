language: rust
cache: cargo

rust:
  - 1.20.0
  - stable
  - beta
  - nightly

script:
  - (cd validator && cargo test --all-features)
  - (cd validator_derive && cargo test --all-features)

notifications:
  email: false
